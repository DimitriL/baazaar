<div class="row">
    {{ form_start(form, {'action': path('baazaar_baazaar_ad_report', {'id' : ad.id}), 'method': 'POST'}) }}
         {{ form_errors(form) }}

         {{ form_rest(form) }}
         <input type="submit" value="Submit">
    {{ form_end(form) }}
</div>
